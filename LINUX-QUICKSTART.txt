╔══════════════════════════════════════════════════════════════╗
║                                                              ║
║    🐧 LET'S ENCRYPT SSL - LINUX QUICK START                 ║
║                                                              ║
╚══════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────┐
│  🚀 FASTEST SETUP (3 Commands)                               │
└──────────────────────────────────────────────────────────────┘

# 1. Edit email
nano setup-letsencrypt-simple-linux.sh
   Change: EMAIL="your-email@domain.com"

# 2. Make executable and run
chmod +x setup-letsencrypt-simple-linux.sh
./setup-letsencrypt-simple-linux.sh

# 3. Done! Access:
https://eyes.indoinfinite.com

✓ Auto-renewal enabled automatically!


┌──────────────────────────────────────────────────────────────┐
│  ✅ PREREQUISITES CHECK                                      │
└──────────────────────────────────────────────────────────────┘

# DNS pointing
dig eyes.indoinfinite.com
# Must return your server IP!

# Firewall open
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Port 80 free
ss -tlnp | grep ':80'
# Should be empty!

# Docker installed
docker --version
docker-compose --version


┌──────────────────────────────────────────────────────────────┐
│  🔧 TROUBLESHOOTING                                          │
└──────────────────────────────────────────────────────────────┘

# Run diagnostic
chmod +x troubleshoot-ssl-linux.sh
./troubleshoot-ssl-linux.sh

# Stop conflicting services
sudo systemctl stop apache2
sudo systemctl stop nginx

# Check logs
tail -f certbot-logs/letsencrypt.log
docker-compose -f docker-compose.ssl.yml logs -f


┌──────────────────────────────────────────────────────────────┐
│  📝 COMMON COMMANDS                                          │
└──────────────────────────────────────────────────────────────┘

Start:      docker-compose -f docker-compose.ssl.yml up -d
Stop:       docker-compose -f docker-compose.ssl.yml down
Logs:       docker-compose -f docker-compose.ssl.yml logs -f
Restart:    docker-compose -f docker-compose.ssl.yml restart nginx

Certificate:
  Check:    docker-compose -f docker-compose.ssl.yml exec certbot certbot certificates
  Renew:    docker-compose -f docker-compose.ssl.yml exec certbot certbot renew


┌──────────────────────────────────────────────────────────────┐
│  🐛 COMMON ISSUES                                            │
└──────────────────────────────────────────────────────────────┘

❌ Port 80 in use:
   sudo systemctl stop apache2
   sudo systemctl stop nginx
   sudo killall -9 nginx

❌ DNS not pointing:
   dig eyes.indoinfinite.com
   # Configure A record at DNS provider

❌ Firewall blocking:
   sudo ufw allow 80/tcp
   sudo ufw allow 443/tcp
   sudo ufw reload

❌ Certificate failed:
   tail -f certbot-logs/letsencrypt.log
   # Check for specific error


┌──────────────────────────────────────────────────────────────┐
│  📚 DOCUMENTATION FILES                                      │
└──────────────────────────────────────────────────────────────┘

LINUX-SSL-GUIDE.md           - Complete Linux guide
LETSENCRYPT-GUIDE.md         - Full documentation
QUICK-REFERENCE.md           - Command reference

Scripts:
  setup-letsencrypt-simple-linux.sh  - Main setup (use this!)
  troubleshoot-ssl-linux.sh          - Diagnostic tool
  quick-ssl-linux.sh                 - One-liner deploy


┌──────────────────────────────────────────────────────────────┐
│  ⚡ ONE-LINER SETUP                                          │
└──────────────────────────────────────────────────────────────┘

# Stop services, get cert, start with SSL
docker-compose down && \
docker run --rm -p 80:80 \
  -v monitoring_letsencrypt-certs:/etc/letsencrypt \
  -v monitoring_letsencrypt-lib:/var/lib/letsencrypt \
  certbot/certbot certonly --standalone \
  --email your-email@domain.com --agree-tos --no-eff-email \
  -d eyes.indoinfinite.com --non-interactive && \
cp nginx/nginx-letsencrypt.conf nginx/nginx.conf && \
docker-compose -f docker-compose.ssl.yml up -d --build


┌──────────────────────────────────────────────────────────────┐
│  🔄 AUTO-RENEWAL                                             │
└──────────────────────────────────────────────────────────────┘

✅ Automatic - No action needed!
   • Checks every 12 hours
   • Renews 30 days before expiry
   • Email notification if fails
   • Certificate valid 90 days

# Verify auto-renewal
docker ps | grep certbot
docker logs monitoring-certbot


┌──────────────────────────────────────────────────────────────┐
│  🎯 EXPECTED RESULT                                          │
└──────────────────────────────────────────────────────────────┘

✅ https://eyes.indoinfinite.com (HTTPS, no port)
✅ No browser security warning
✅ Valid certificate (Let's Encrypt)
✅ HTTP → HTTPS redirect
✅ Auto-renewal enabled
✅ SSL Labs Grade: A or A+


┌──────────────────────────────────────────────────────────────┐
│  🆘 EMERGENCY                                                │
└──────────────────────────────────────────────────────────────┘

# HTTPS not working
docker-compose -f docker-compose.ssl.yml restart nginx
docker-compose -f docker-compose.ssl.yml logs nginx

# Certificate expired
docker-compose -f docker-compose.ssl.yml exec certbot certbot renew --force-renewal
docker-compose -f docker-compose.ssl.yml restart nginx

# Complete reset
docker-compose -f docker-compose.ssl.yml down
docker volume rm monitoring_letsencrypt-certs
./setup-letsencrypt-simple-linux.sh


═══════════════════════════════════════════════════════════════

              📖 Full Guide: LINUX-SSL-GUIDE.md

═══════════════════════════════════════════════════════════════
